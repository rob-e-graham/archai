# Huggle relay — Claude (cloud/web) → #archai

Cloud/web Claude cannot reach the local Huggle bus (`localhost:4848` is on the Mac; this session
runs in an isolated container with no route to it). It hands off through git. A **Mac-local agent**
(Codex, or a terminal Claude with `HUGGLE_URL`/`HUGGLE_AGENT` set) should relay the message below into
Huggle after a `git pull`, then delete or archive this file.

**Channel:** `#archai`
**Protocol note:** this is a `post summary` step for work already done + verified in git; no open claim to release.

---

```
huggle_post #archai "Claude (cloud/web) handoff via git relay — cannot reach the bus directly.

DONE (branch claude/archai-app-improvements-lmz0n1, pushed, draft PR #9):
• Reliability fail-safe — backend-archai/scripts/archai-stack-supervisor.sh keeps Docker+Qdrant alive and FLAGS any outage (macOS notification + optional ARCHAI_ALERT_WEBHOOK). install-public-launchagents.sh now also installs a caffeinate (keep-awake) agent + the supervisor, alongside backend + cloudflared. Root cause of the recurring 'objects won't load': Docker/Qdrant were unsupervised and the Mac slept.
• Hardened the supervisor to run on stock macOS bash 3.2 (removed declare -A associative array → file-based state; Qdrant check tolerant of /readyz vs /). Syntax + transition logic tested.
• Phone-nav demo nfc-pages/v/exhibition-map.html — six real AUXIO objects on the routed wayfinding engine.
• Ethical-models direction made concrete in data-residency policy §11 (OLMo 2 / Pythia / IBM Granite / PleIAs Common Corpus / Fairly Trained), matching the essay.
• ARCHAI_PROGRESS.md updated to 2026-07-13 + session handover + resume header.

VERIFY before outreach (Mac-side, detailed in ARCHAI_PROGRESS.md PRE-OUTREACH block):
1) install launchagents; 2) 403 lockdown test on /api/comments; 3) live build = v11.6.15; 4) objects load.

OPEN DECISION: Obtext IP — protect vs open prior-art. Keep 'Obtext' out of public replies/pitches until decided.

Cloud Claude's channel is git; Mac-local agent please relay + mirror future handoffs."
```
