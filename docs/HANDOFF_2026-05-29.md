# ARCHAI Handoff — 2026-05-29

## Focus of this pass

This pass was about getting the main experience back onto the same page:

- AUX.IO public object pages working again
- ARCHAI app prompt flows working in the in-app browser
- AUX.IO styling aligned with the website accent treatment
- website surfaces and app surfaces no longer contradicting each other

## What changed

### AUX.IO generation and runtime

- `nfc-pages/nfc-visitor-template.html`
  - Fixed the broken direct-mode `SYSTEM_PROMPT` template literal that was generating invalid JavaScript in the published AUX.IO pages.
  - Added the aqua accent treatment to the chat row:
    - softer aqua border
    - subtle aqua background tint
    - aqua-tinted `Ask anything…` placeholder
  - Added a `fetchWithTimeout(...)` helper so chat falls back gracefully instead of sitting on `Thinking…` indefinitely.
- `nfc-pages/generate-nfc-pages.js`
  - The generator is currently producing the full live set of 775 unique AUX.IO pages from 6 live collections.
- `nfc-pages/v/`
  - Regenerated the AUX.IO visitor page set after the template/runtime fixes.
  - Current output: `775` pages.
- `backend-archai/src/server.js`
  - The AUX.IO manifest/random-page logic is using the live generated page set rather than the old small hardcoded subset.

### Main ARCHAI app

- `ARCHAI_v10_8.html`
  - Replaced browser `prompt()` dependencies with an internal modal prompt helper (`appPrompt(...)`).
  - Updated these flows to use the modal prompt:
    - batch tagging in Curator
    - FAMTEC post creation
    - FAMTEC direct enquiry fallback
    - local vocabulary mapping creation
  - Updated the top comment header to `All rights reserved` so the visible app file no longer advertises MPL at the top.

### Runtime state at verification time

- `http://localhost:6333` (Qdrant): live
- `http://localhost:8787/api/health`: live
- `http://localhost:11434`: live
- backend health during verification reported:
  - `collectionObjects: 815`
  - `qdrantCollections: 6`
  - `curatorVectors: 815`
  - `totalVectors: 1630`

## Verified

Smoke-tested with Playwright on 2026-05-29 using the temp QA harness in `/tmp/famtec-check`:

```bash
cd /tmp/famtec-check
npx playwright test site-smoke.spec.js --config=playwright.config.js --reporter=line
```

Passing checks:

- homepage updates locally
- public AUX.IO wrapper loads live status and rotates objects
- local AUX.IO object pages render the updated chat styling and stop hanging indefinitely
- local ARCHAI public page links cleanly to AUX.IO instead of embedding it
- SafeChat and Keytec screenshot cleanup is reflected locally
- main ARCHAI app uses the internal modal prompt instead of browser `prompt()`

## Important current numbers

- Live public AUX.IO page count: `775`
- Backend source-object count: `815`

Why they differ:

- `archai_pilot` contains `80` source points but only `40` unique public-facing canonical objects.
- Public AUX.IO pages are deduped by canonical object identity, not raw vector count.

## Open follow-up items

### Licensing alignment

There is still a repo-level licensing mismatch that should be resolved deliberately:

- website ARCHAI/AUX surfaces now say `All rights reserved`
- parts of the ARCHAI repo still refer to MPL / trademark-era licensing:
  - `README.md`
  - `NOTICE`
  - `LICENSE`
  - `backend-archai/package.json`
  - `docs/ARCHAI_UPDATE_2026-05.md`

That should be handled in a dedicated licensing pass so the public website, repo metadata, and app headers all agree.

### Optional next technical cleanup

- Rename `ARCHAI_v10_8.html` to a true `v10.9` filename once the surrounding launch scripts and manifest entries are updated together.
- Replace remaining browser `alert()` / `confirm()` calls with the same internal modal system for full consistency.
- Add a committed QA folder or scripted smoke test instead of relying on the temp `/tmp/famtec-check` harness.
